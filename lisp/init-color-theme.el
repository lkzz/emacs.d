;;; init-color-theme.el --- setup theme
;;; Commentary:
;;; Code:

(use-package zenburn-theme)
(use-package monokai-theme)

;; (load-theme 'zenburn t)
(load-theme 'monokai t)

(provide 'init-color-theme)
;;; init-color-theme ends here
